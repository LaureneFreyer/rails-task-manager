<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p class="status"><%= @status %></p>

<%= link_to 'Edit', edit_task_path, class:'link' %>
<%= link_to 'Delete', task_path(@task), class: 'link', data: {turbo_method: :delete, turbo_confirm: "Really want to delete?"} %>

<%= link_to 'Back', tasks_path, class: 'link' %>
